#include "main.h"

/**
 * mul - multiplies two int
 * @a: int must be multiplied to b
 * @b: int must be multiplied to a
 * Return: the result of the operation
 */
int mul(int a, int b)
{
	int c;

	c = a * b;

	return (c);
}
